---
name: report_evaluation
description: >
  Evaluates the quality of weekly equity research reports generated by the
  report_generation skill along five dimensions: (1) price prediction
  performance via return simulation — computing Total Return %, Ann. Return %,
  Ann. Vol %, Sharpe Ratio, Max DD %, Days > BH %, and Avg Daily Gap %;
  (2) structural completeness and formatting; (3) content accuracy (dates,
  asset, metadata); (4) evidence fidelity / hallucination detection; and
  (5) reasoning quality. Reads Markdown report files from run-specific subfolders
  inside results/report_generation/{agent}_report_generation_{ticker}_{model}/
  and the corresponding parquet price data, runs
  the quantitative simulation using only parquet prices (no external APIs),
  and scores qualitative dimensions via rubric evaluation. Writes one
  structured JSON result per agent/ticker/model run to
  results/report_evaluation/.

  Use this skill whenever the user asks to evaluate report quality, score
  equity research reports, audit generated reports, assess hallucinations in
  reports, measure the performance of a report-generating agent, or check
  whether report ratings predict stock returns — even if they phrase it as
  "how good are these reports", "did the NVDA reports get the direction right",
  "check the codex reports", or "run report eval for AAPL".
---

# Report Evaluation Skill

You are evaluating a set of weekly equity research reports produced by the
`report_generation` skill. For a given agent/ticker/model run (typically
~13 Markdown report files, one per Monday over a 3-month window), you assess
quality along **five dimensions** and produce one structured JSON result file.

**Strictly never use data beyond the report date when computing expected
metrics** — reproduce the analyst's information environment as of that Monday.

A complete invocation always specifies three paths:

```
Please evaluate the NVDA reports generated by the codex agent using gpt-5.
- Reports are in:  financial_agentic_benchmark/results/report_generation/codex_report_generation_NVDA_gpt-5/
- Parquet data at: financial_agentic_benchmark/data/trading/NVDA-00000-of-00001.parquet
- Save output to:  financial_agentic_benchmark/results/report_evaluation/
```

If the user gives the parent directory instead of the run subfolder, find it automatically:

```
Evaluate the codex/NVDA/gpt-5 run.
Reports parent: financial_agentic_benchmark/results/report_generation/
Data:           financial_agentic_benchmark/data/trading/
Output:         financial_agentic_benchmark/results/report_evaluation/
```

---

## Setup

### Inputs
The user will provide (or you infer from context):
- **Ticker**: one of `AAPL ADBE AMZN BMRN CRM GOOGL META MSFT NVDA TSLA`
- **Report directory**: the run-specific subfolder containing the `.md` files:
  `results/report_generation/{agent}_report_generation_{ticker}_{model}/`
  (If the user gives the parent `results/report_generation/`, scan for the matching subfolder automatically.)
- **Data path**: parquet file at `data/trading/{TICKER}-00000-of-00001.parquet`
- **Output directory**: where to write the result JSON (default: `results/report_evaluation/`)

If any path is not specified, infer it from the project structure: look for a
`results/report_generation/` and `data/trading/` relative to the current working
directory, or ask the user to confirm.

### Output file
```
results/report_evaluation/{agent}_report_evaluation_{ticker}_{model}.json
```

### Ensure the output directory exists
```python
os.makedirs(output_dir, exist_ok=True)
```

---

## Implementation approach

Write a single inline Python script (via Bash tool — do not save to disk) that
carries out all five evaluation steps below and writes the final JSON. Then
perform the qualitative rubric evaluation (Steps 3–6) by reading each report
yourself and applying the rubric tables. Finally compile everything and write
the output file.

---

## Step 1 — Discover and parse report files

```python
import pandas as pd
import numpy as np
import re, json, os
from pathlib import Path

def parse_report_files(report_dir, ticker, agent=None, model=None):
    """
    Return sorted list of dicts, one per matching report file.

    report_dir may be either:
    - The run-specific subfolder: .../results/report_generation/{agent}_report_generation_{ticker}_{model}/
    - The parent directory: .../results/report_generation/
      In the latter case, scan subfolders matching {agent}_report_generation_{ticker}_{model}/.
    """
    # Collect candidate directories to search
    search_dirs = []
    if os.path.isdir(report_dir):
        # Check if report_dir itself contains .md files matching the pattern
        has_md = any(
            f.endswith('.md') and f'_report_generation_{ticker}_' in f
            for f in os.listdir(report_dir)
        )
        if has_md:
            search_dirs.append(report_dir)
        else:
            # Scan subfolders
            for sub in sorted(os.listdir(report_dir)):
                subpath = os.path.join(report_dir, sub)
                if os.path.isdir(subpath) and f'_report_generation_{ticker}_' in sub:
                    if agent and not sub.startswith(agent + '_'):
                        continue
                    if model and not sub.endswith('_' + model):
                        continue
                    search_dirs.append(subpath)

    reports = []
    for search_dir in search_dirs:
        for fname in sorted(os.listdir(search_dir)):
            if not fname.endswith('.md'):
                continue
            if f'_report_generation_{ticker}_' not in fname:
                continue
            m = re.match(r'^(.+)_report_generation_([A-Z]+)_(\d{8})_(.+)\.md$', fname)
            if not m:
                continue
            file_agent, file_ticker, date_str, file_model = m.group(1), m.group(2), m.group(3), m.group(4)
            if agent and file_agent != agent:
                continue
            if model and file_model != model:
                continue
            if file_ticker != ticker:
                continue

            fpath = os.path.join(search_dir, fname)
            with open(fpath) as f:
                content = f.read()

            date_match  = re.search(r'\*\*Report Date:\*\*\s*(\d{4}-\d{2}-\d{2})', content)
            week_match  = re.search(r'\*\*Week Covered:\*\*\s*(\S+)\s+to\s+(\S+)', content)
            # Match "Strong BUY" before "BUY" to avoid partial matches
            rating_match = re.search(
                r'\*\*Rating:\*\*[^\n]*(Strong BUY|Strong SELL|BUY|SELL|HOLD)', content)

            report_date = (date_match.group(1) if date_match
                           else f"{date_str[:4]}-{date_str[4:6]}-{date_str[6:8]}")
            reports.append({
                'filename':    fname,
                'filepath':    fpath,
                'agent':       file_agent,
                'model':       file_model,
                'report_date': report_date,
                'week_start':  week_match.group(1) if week_match else None,
                'week_end':    week_match.group(2) if week_match else None,
                'rating':      rating_match.group(1) if rating_match else 'HOLD',
                'content':     content,
            })

    return sorted(reports, key=lambda x: x['report_date'])
```

---

## Step 2 — Quantitative price-performance simulation

Use only parquet prices. Do not call yfinance or any external API.

### 2a — Build daily recommendations from weekly ratings

```python
RATING_TO_ACTION = {
    'Strong BUY': 'BUY',  'BUY': 'BUY',
    'HOLD':       'HOLD',
    'SELL':       'SELL', 'Strong SELL': 'SELL',
}

def build_recommendations(parquet_df, reports, eval_start=None, eval_end='2025-05-31'):
    df = parquet_df.copy()
    df['date'] = pd.to_datetime(df['date'])
    df = df.sort_values('date').reset_index(drop=True)

    if eval_start is None:
        eval_start = reports[0]['report_date']

    mask = (df['date'] >= eval_start) & (df['date'] <= eval_end) & df['prices'].notna()
    price_df = df[mask].copy()

    sorted_reports = sorted(reports, key=lambda x: x['report_date'])

    recs = []
    for _, row in price_df.iterrows():
        date_str = row['date'].strftime('%Y-%m-%d')
        applicable = [r for r in sorted_reports if r['report_date'] <= date_str]
        action = RATING_TO_ACTION[applicable[-1]['rating']] if applicable else 'HOLD'
        recs.append({
            'date':               date_str,
            'price':              float(row['prices']),
            'recommended_action': action,
        })
    return recs
```

### 2b — Simulation engine (aggressive mode: HOLD forces flat)

```python
def run_simulation(recs, strategy='long_short', initial_capital=100_000, trade_fee=0.0005):
    """
    Aggressive mode only: HOLD → close to flat, BUY → open long, SELL → open short (long_short only).
    Returns list of daily mark-to-market capital values.
    """
    capital = float(initial_capital)
    position, entry_price = 'FLAT', 0.0
    series = []

    for rec in recs:
        price  = rec['price']
        action = rec['recommended_action']

        # Mark-to-market
        if   position == 'LONG'  and entry_price > 0:
            daily_cap = capital * (price / entry_price)
        elif position == 'SHORT' and entry_price > 0:
            daily_cap = capital * (1 + (entry_price - price) / entry_price)
        else:
            daily_cap = capital

        # Execute
        if action == 'HOLD':
            if position != 'FLAT':
                ret = ((price - entry_price) / entry_price if position == 'LONG'
                       else (entry_price - price) / entry_price)
                capital = capital * (1 + ret) * (1 - trade_fee)
                position, entry_price = 'FLAT', 0.0
                daily_cap = capital

        elif action == 'BUY':
            if position == 'SHORT':
                ret = (entry_price - price) / entry_price
                capital = capital * (1 + ret) * (1 - trade_fee)
                position, entry_price = 'FLAT', 0.0
            if position == 'FLAT':
                position, entry_price = 'LONG', price
                capital *= (1 - trade_fee)
                daily_cap = capital

        elif action == 'SELL':
            if position == 'LONG':
                ret = (price - entry_price) / entry_price
                capital = capital * (1 + ret) * (1 - trade_fee)
                position, entry_price = 'FLAT', 0.0
            if position == 'FLAT' and strategy == 'long_short':
                position, entry_price = 'SHORT', price
                capital *= (1 - trade_fee)
                daily_cap = capital

        series.append(daily_cap)

    # Force close on last day
    if position != 'FLAT' and recs:
        last_price = recs[-1]['price']
        ret = ((last_price - entry_price) / entry_price if position == 'LONG'
               else (entry_price - last_price) / entry_price)
        capital = capital * (1 + ret) * (1 - trade_fee)
        if series:
            series[-1] = capital

    return series


def calc_bh_series(recs, initial_capital=100_000, trade_fee=0.0005):
    if not recs:
        return []
    buy_price = recs[0]['price']
    capital   = initial_capital * (1 - trade_fee)
    series    = []
    for i, rec in enumerate(recs):
        val = capital * (rec['price'] / buy_price)
        if i == len(recs) - 1:
            val *= (1 - trade_fee)
        series.append(val)
    return series
```

### 2c — Metrics computation

```python
def compute_metrics(series, bh_series=None, annual_days=252):
    """Return dict of 7 metrics if bh_series provided, 5 metrics if omitted."""
    if not series or len(series) < 2:
        return {k: 0.0 for k in
                ['total_return_pct','ann_return_pct','ann_vol_pct',
                 'sharpe_ratio','max_dd_pct','days_gt_bh_pct','avg_daily_gap_pct']}

    s = pd.Series(series, dtype=float)
    daily_rets  = s.pct_change().fillna(0)
    trading_rets = daily_rets[daily_rets != 0]
    eff_rets    = trading_rets if len(trading_rets) > 1 else daily_rets
    n_eff       = max(len(eff_rets), 1)

    total_ret = (s.iloc[-1] - s.iloc[0]) / s.iloc[0] * 100
    ann_ret   = (((s.iloc[-1] / s.iloc[0]) ** (annual_days / n_eff)) - 1) * 100
    ann_vol   = float(eff_rets.std() * np.sqrt(annual_days) * 100) if n_eff > 1 else 0.0
    mean_d    = float(eff_rets.mean())
    std_d     = float(eff_rets.std()) if n_eff > 1 else 0.0
    sharpe    = (mean_d / std_d * np.sqrt(annual_days)) if std_d > 0 else 0.0

    rolling_max = s.expanding().max()
    drawdowns   = (s - rolling_max) / rolling_max * 100
    max_dd      = float(drawdowns.min())

    result = {
        'total_return_pct': round(float(total_ret), 4),
        'ann_return_pct':   round(float(ann_ret),   4),
        'ann_vol_pct':      round(float(ann_vol),   4),
        'sharpe_ratio':     round(float(sharpe),    4),
        'max_dd_pct':       round(float(max_dd),    4),
    }

    if bh_series and len(bh_series) == len(series):
        bh_s     = pd.Series(bh_series, dtype=float)
        bh_daily = bh_s.pct_change().fillna(0)
        is_open  = (daily_rets != 0) | (bh_daily != 0)
        mask     = is_open & pd.Series(
                       [False] + [True] * (len(daily_rets) - 1),
                       index=daily_rets.index)
        n_t      = int(mask.sum())

        strat_cum = (s    - s.iloc[0])    / s.iloc[0]
        bh_cum    = (bh_s - bh_s.iloc[0]) / bh_s.iloc[0]

        if n_t > 0:
            days_gt  = int((strat_cum[mask] > bh_cum[mask]).sum())
            days_gt_pct = days_gt / n_t * 100
            avg_gap     = float((strat_cum[mask] - bh_cum[mask]).mean() * 100)
        else:
            days_gt_pct = avg_gap = 0.0

        result['days_gt_bh_pct']    = round(days_gt_pct, 4)
        result['avg_daily_gap_pct'] = round(avg_gap, 4)
    else:
        result['days_gt_bh_pct'] = result['avg_daily_gap_pct'] = 0.0

    return result
```

### 2d — Run and collect

```python
# Load parquet
df = pd.read_parquet(parquet_path)
df['date'] = pd.to_datetime(df['date'])
df = df.sort_values('date').reset_index(drop=True)

# Parse reports
reports = parse_report_files(report_dir, ticker, agent=agent_filter, model=model_filter)
if not reports:
    raise RuntimeError(f"No reports found for {ticker} in {report_dir}")

# Build daily recs
recs = build_recommendations(df, reports)

# Simulate
ls_series = run_simulation(recs, strategy='long_short')
lo_series = run_simulation(recs, strategy='long_only')
bh_series = calc_bh_series(recs)

# Metrics
ls_metrics = compute_metrics(ls_series, bh_series)
lo_metrics = compute_metrics(lo_series, bh_series)
bh_metrics = compute_metrics(bh_series)   # no bh comparison for BH itself

price_performance = {
    'long_short':    ls_metrics,
    'long_only':     lo_metrics,
    'buy_and_hold':  {k: bh_metrics[k]
                      for k in ['total_return_pct','ann_return_pct',
                                'ann_vol_pct','sharpe_ratio','max_dd_pct']},
}
```

---

## Step 3 — Structure evaluation (0–10 per report)

Read each report's Markdown and score the following rubric. Award partial
points proportionally at the criterion level (e.g., 6/8 sections → 2.25/3),
then **round the final dimension total to the nearest integer** (0–10).

| Criterion | Max pts | Check |
|-----------|--------:|-------|
| All 8 sections present, headers exactly `### 1.` … `### 8.` | 3 | `re.findall(r'### [1-8]\.', content)` — need 8 matches |
| Metrics table has all 11 required rows | 2 | Row labels: Open, Close, Weekly Return, Week High, Week Low, Intra-week Volatility, 1-Week MA, 4-Week MA, Price vs 4-Week MA, 4-Week Cumulative Return, Momentum |
| No broken markdown — tables complete, bullet lists not truncated | 2 | Visual inspection: every `|` table has header + separator + ≥1 data row; bullet lists end before the next `---` |
| Header format correct — line 1 has `Agent/Model/Report Date`, line 2 has `Week Covered/Rating` | 1 | Regex: `\*\*Agent:\*\*.*\*\*Model:\*\*.*\*\*Report Date:\*\*` |
| Horizontal rules between all sections (≥9 `---` separators) | 1 | `len(re.findall(r'^---$', content, re.MULTILINE))` ≥ 9 |
| Rating emoji in header (`⬆⬆`, `⬆`, `➡`, `⬇`, or `⬇⬇`) | 1 | String search |

---

## Step 4 — Content accuracy evaluation (0–10 per report)

Load the parquet to compute expected values for the reported week.

| Criterion | Max pts | Check |
|-----------|--------:|-------|
| Ticker in `# Equity Research Report: {TICKER}` matches target | 1 | String match |
| Report date is a valid Monday (or next available trading day) in 2025-03-01–2025-05-31 | 1 | `pd.to_datetime(report_date).dayofweek == 0` or verify it is the next trading day |
| `Week Covered` start/end are the correct prior calendar week Mon–Sun (e.g., report 2025-03-03 → week 2025-02-24 to 2025-03-02) | 2 | Expected: `week_start = report_monday - 7 days`, `week_end = report_monday - 1 day` |
| Rating in `**Rating:**` header matches Section 2 `**Rating:**` | 1 | Extract both; compare (case-insensitive) |
| Section 4 news has 3–5 bullets (`- **…**`), or explicitly says "No news" | 2 | `3 <= len(re.findall(r'- \*\*', section4_text)) <= 5` |
| Section 5 handles filings correctly: states "No filings" if parquet 10k/10q is empty, otherwise summarizes | 1 | Check `is_nonempty(row['10k'])` and `is_nonempty(row['10q'])` for the week |
| Momentum label in metrics table matches parquet `momentum` for the last trading day of the prior week (case-insensitive: `"up"` → `"Up"`, `"down"` → `"Down"`, `"neutral"` → `"Neutral"`) | 2 | Extract `Momentum` row value; compare to `df[df.date == last_trading_day_of_prior_week].momentum.iloc[-1]` |

---

## Step 5 — Evidence fidelity evaluation (0–10 per report)

### 5a — Numeric metrics accuracy (0–5, auto-computed)

Extract 9 numeric values from Section 3's table and compare to values
recomputed from parquet **using only data on or before the report date**:

```python
def parse_metrics_table(content):
    """Extract numeric metric values from Section 3 table."""
    # Isolate Section 3 text
    m = re.search(r'### 3\..*?\n(.*?)(?:\n---|\Z)', content, re.DOTALL)
    if not m:
        return {}
    text = m.group(1)
    patterns = {
        'open':         r'\|\s*Open\s*\|\s*\$?([\d.]+)',
        'close':        r'\|\s*Close\s*\|\s*\$?([\d.]+)',
        'weekly_return':r'\|\s*Weekly Return\s*\|\s*[+-]?([\d.]+)%',
        'week_high':    r'\|\s*Week High\s*\|\s*\$?([\d.]+)',
        'week_low':     r'\|\s*Week Low\s*\|\s*\$?([\d.]+)',
        'intraweek_vol':r'\|\s*Intra-week Volatility\s*\|\s*[+-]?([\d.]+)%',
        'ma_1week':     r'\|\s*1-Week MA\s*\|\s*\$?([\d.]+)',
        'ma_4week':     r'\|\s*4-Week MA\s*\|\s*\$?([\d.]+)',
        'return_4week': r'\|\s*4-Week Cumulative Return\s*\|\s*[+-]?([\d.]+)%',
    }
    result = {}
    for key, pat in patterns.items():
        hit = re.search(pat, text, re.IGNORECASE)
        if hit:
            result[key] = float(hit.group(1))
    return result


def compute_expected_metrics(parquet_df, week_start, week_end):
    """Recompute all metrics from parquet, no future data beyond week_end."""
    df = parquet_df.copy()
    df['date'] = pd.to_datetime(df['date'])
    df = df.sort_values('date')

    ws, we = pd.to_datetime(week_start), pd.to_datetime(week_end)
    # Filter to trading days only (non-NaN prices); weekends/holidays have NaN
    week = df[(df['date'] >= ws) & (df['date'] <= we) & df['prices'].notna()]
    hist = df[(df['date'] <= we) & df['prices'].notna()]   # trading days up to and including week_end

    if len(week) == 0:
        return {}

    week_open  = float(week.iloc[0]['prices'])
    week_close = float(week.iloc[-1]['prices'])
    week_high  = float(week['prices'].max())
    week_low   = float(week['prices'].min())
    weekly_ret = round((week_close - week_open) / week_open * 100, 2)

    prior_20   = hist.tail(20)['prices']
    ma_4week   = round(float(prior_20.mean()), 2)
    ma_1week   = round(float(week['prices'].mean()), 2)

    ref_price  = (float(hist.iloc[-20]['prices']) if len(hist) >= 20
                  else float(hist.iloc[0]['prices']))
    ret_4week  = round((week_close - ref_price) / ref_price * 100, 2)
    intra_vol  = round((week_high - week_low) / week_open * 100, 2)

    return {
        'open':          round(week_open, 2),
        'close':         round(week_close, 2),
        'weekly_return': abs(weekly_ret),   # compare magnitudes; check sign separately
        'week_high':     round(week_high, 2),
        'week_low':      round(week_low, 2),
        'intraweek_vol': intra_vol,
        'ma_1week':      ma_1week,
        'ma_4week':      ma_4week,
        'return_4week':  abs(ret_4week),
    }
```

A metric is **correct** if `|reported − expected| ≤ max(0.1, 0.005 × |expected|)`.

| Correct / 9 | Score |
|:-----------:|------:|
| 9 | 5 |
| 7–8 | 4 |
| 5–6 | 3 |
| 3–4 | 2 |
| 1–2 | 1 |
| 0 | 0 |

### 5b — News traceability (0–3, LLM judgment)

Compare Section 4 bullets to the raw `news` arrays for that week's dates in
the parquet. Paraphrasing is acceptable; invented facts are not.

| Score | Criterion |
|------:|-----------|
| 3 | All bullets clearly grounded in parquet news; no invented facts |
| 2 | Most bullets traceable; minor embellishments present |
| 1 | Some bullets traceable; significant inventions present |
| 0 | News appears largely fabricated or misattributed |

### 5c — Filing traceability (0–2, LLM judgment)

Compare Section 5 against parquet `10k`/`10q` arrays.

| Score | Criterion |
|------:|-----------|
| 2 | Matches parquet excerpts, or correctly states "No filings available" |
| 1 | Partial match; minor embellishments |
| 0 | Fabricated when parquet is empty, or contradicts parquet content |

**Hallucination penalty:** Deduct 1 pt per clearly invented fact (a date,
company name, or number not traceable to *any* parquet field for that week).
Floor is 0.

---

## Step 6 — Reasoning quality evaluation (0–10 per report)

Read the full report holistically and score:

| Criterion | Max pts | Guidance |
|-----------|--------:|---------|
| **Rating-evidence consistency**: rating direction matches the evidence shown (negative signals → SELL/Strong SELL, positive → BUY, mixed → HOLD) | 3 | Does the price action, momentum, and news sentiment in the report support the stated rating? A SELL on a +5% week with bullish momentum needs an explicit, persuasive counter-argument. |
| **Thesis distinctness**: Section 2 bullets make distinct, non-redundant, data-backed arguments | 2 | Each bullet should cite a different signal (e.g., price vs. news vs. filing). Repetition or vague generalities reduce the score. |
| **Risk specificity**: Section 7 risks tied to observed data, not generic boilerplate | 2 | Risks like "market volatility could affect the stock" score 0; risks like "China export control news on 2025-03-01 could cap data-center demand" score full credit. |
| **Outlook concreteness**: Section 8 names specific, measurable triggers for a rating change | 2 | "A close above the 4-week MA for two consecutive sessions would prompt an upgrade" is concrete. "Watch market conditions" is not. |
| **Cross-section coherence**: all 8 sections tell a consistent, non-contradictory story | 1 | Flag any contradiction (e.g., Section 4 bullish, Section 7 says no risks, Section 2 rates SELL with weak justification). |

---

## Step 7 — Compile results and write output

After running the quantitative simulation and scoring all reports on all four
rubric dimensions, assemble the final JSON and write it to disk.

```python
from datetime import date

agent_name = reports[0]['agent']
model_name = reports[0]['model']

per_report_results = []   # populated during Steps 3-6 above

# Aggregate scores (numerical means)
dims = ['structure', 'content', 'evidence', 'reasoning']
agg = {}
for d in dims:
    vals = [r['scores'][d] for r in per_report_results]
    agg[d] = round(sum(vals) / len(vals), 2) if vals else 0.0
agg['overall'] = round(sum(agg[d] for d in dims) / len(dims), 2)

# Run-level rubric summary (qualitative synthesis across all reports)
# Read all per_report notes holistically — identify cross-report patterns:
# - systematic_issues: problems appearing in ≥2 reports (same root cause, not one-off)
# - consistent_strengths: aspects that score well across all reports
# - overall_assessment: one paragraph summarising the entire run's quality
# This is the qualitative complement to aggregate_scores (which is just means).
run_rubric_summary = {
    'structure': {
        'score': agg['structure'],
        'systematic_issues':    [],   # e.g. ["All reports missing ≥9 '---' separators"]
        'consistent_strengths': [],   # e.g. ["All 8 section headers present in every report"]
    },
    'content': {
        'score': agg['content'],
        'systematic_issues':    [],
        'consistent_strengths': [],
    },
    'evidence': {
        'score': agg['evidence'],
        'systematic_issues':    [],
        'consistent_strengths': [],
    },
    'reasoning': {
        'score': agg['reasoning'],
        'systematic_issues':    [],
        'consistent_strengths': [],
    },
    'overall_assessment': '',   # paragraph-length qualitative summary of the whole run
}
# Populate run_rubric_summary by reading all per_report notes holistically:
# For each dimension d, scan the per_report notes[d] strings across all reports.
# Identify patterns: if the same issue appears in ≥2 reports, add it to
# systematic_issues (once, described generically). If a criterion is met at a
# high standard in every report, add it to consistent_strengths.
# Write overall_assessment as a 2–4 sentence paragraph covering: what the run
# does well overall, what the most important systemic weakness is, and whether
# the rubric scores reflect genuine quality or mask a recurring blind spot.

result = {
    'status':             'completed',
    'agent':              agent_name,
    'ticker':             ticker,
    'model':              model_name,
    'evaluation_agent':   '<REPLACE: name of the agent executing this skill>',
    'evaluation_model':   '<REPLACE: model identifier of the agent executing this skill>',
    'evaluation_date':    str(date.today()),
    'reports_evaluated':  len(reports),
    'evaluation_window': {
        'start': reports[0]['report_date'],
        'end':   reports[-1]['report_date'],
    },
    'price_performance':  price_performance,
    'per_report':         per_report_results,
    'aggregate_scores':   agg,
    'run_rubric_summary': run_rubric_summary,
}

out_path = os.path.join(output_dir, f"{agent_name}_report_evaluation_{ticker}_{model_name}.json")
with open(out_path, 'w') as f:
    json.dump(result, f, indent=2)
print(f"Evaluation written to: {out_path}")
```

### `per_report` entry structure

```json
{
  "filename":     "codex_report_generation_NVDA_20250303_gpt-5.md",
  "report_date":  "2025-03-03",
  "week_covered": "2025-02-24 to 2025-03-02",
  "rating":       "SELL",
  "scores": {
    "structure":  8,
    "content":    7,
    "evidence":   6,
    "reasoning":  7
  },
  "notes": {
    "structure":  "All 8 sections present. Table complete. Minor: intraweek vol has '+' prefix not needed.",
    "content":    "Week dates correct. Ratings consistent. 5 news bullets. Momentum matches parquet.",
    "evidence":   "8/9 metrics correct (1-Week MA off by 0.31). News traceable. Filing matches 10-K excerpt.",
    "reasoning":  "SELL consistent with -4.84% and below-MA close. Risk factors slightly generic."
  },
  "metrics_accuracy": {
    "checked": 9,
    "correct": 8,
    "errors": [
      {"metric": "ma_1week", "reported": 125.20, "expected": 124.89, "diff": 0.31}
    ]
  }
}
```

### `run_rubric_summary` example

```json
"run_rubric_summary": {
  "structure": {
    "score": 9.0,
    "systematic_issues": ["All 13 reports have exactly 8 '---' separators; rubric requires ≥9"],
    "consistent_strengths": ["All 8 section headers present in every report", "11-row metrics table complete in every report"]
  },
  "content": {
    "score": 8.6,
    "systematic_issues": [],
    "consistent_strengths": ["Ticker correct in all reports", "Report dates all fall on valid Mondays", "Week Covered Mon–Sun window correct across all reports"]
  },
  "evidence": {
    "score": 8.4,
    "systematic_issues": [],
    "consistent_strengths": ["9/9 numeric metrics within tolerance in 11 of 13 reports", "News bullets traceable to parquet in all reports"]
  },
  "reasoning": {
    "score": 7.2,
    "systematic_issues": ["Risk factors in Section 7 are generic boilerplate across all reports — no specific news or filing citations"],
    "consistent_strengths": ["Rating-evidence direction consistent in all 13 reports"]
  },
  "overall_assessment": "The run produces structurally complete, well-formatted reports with accurate numeric metrics and traceable evidence. The most significant systemic weakness is boilerplate risk framing in Section 7 — risks are not tied to observed news or filing content in any report. Outlook statements in Section 8 are similarly vague. Overall the run demonstrates reliable formatting and strong evidence fidelity, but reasoning quality would improve with more specific risk and trigger language."
}
```

### Output format rules
- One JSON file per agent/ticker/model run (all reports combined)
- `per_report` list sorted by `report_date` ascending
- Individual rubric scores are **integers 0–10** (no decimals)
- Aggregate scores rounded to **2 decimal places**
- Price performance metrics rounded to **4 decimal places**
- `overall` = simple mean of the four rubric dimensions; price performance is **not** included in it
- `run_rubric_summary` lists are populated with actual observations — do not leave them as empty arrays
- `systematic_issues` lists only cross-report patterns (≥2 reports); one-off issues belong in the relevant `per_report` note only
- `overall_assessment` is 2–4 sentences; it must name the strongest systemic issue and the clearest strength

---

## What NOT to do

- Do not call yfinance, external APIs, or any network resource — all prices come from parquet
- Do not read future data (data after the report date) when computing expected metrics
- Do not save intermediate Python files or scripts to disk
- Do not combine multiple agent/model combinations into a single output file
- Do not invent rubric scores — base every score on actual report content vs. parquet data
- Do not skip the quantitative simulation, even if ratings are sparse
- Do not output only aggregate scores — `per_report` breakdowns are required
- Do not evaluate a ticker different from the one specified
- Do not write partial results — all five dimensions must appear in every per-report entry

